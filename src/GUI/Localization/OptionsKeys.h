#ifndef __OPTIONS_KEYS__
#define __OPTIONS_KEYS__
// ogolne klucze lokalizacyjne

#include "KeyMacros.h"

namespace Locale{

KEY(OPT_RESOLUTION)

KEY(OPT_FULLSCREEN)

KEY(OPT_VSYNC)

KEY(OPT_SOUND_LEVEL)

KEY(OPT_MUSIC_LEVEL)

KEY(OPT_CONTROLS)

KEY(OPT_SAVE_EXIT)

KEY(OPT_CANCEL)

}

#endif

